import React from 'react';

const CartWidget = () => {
    return (
        <div>
            {/* Icono o texto del carrito */}
            <span>0</span>
        </div>
    );
};

export default CartWidget;